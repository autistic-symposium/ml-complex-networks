'''

A graph may have multiple cycles. However, each cycle in the graph can be decomposed into the sum (defined as "exclusive or" or the edges) of several cycles. We call a minimal collection of such cycles as a cycle basis. The cardiality of cycle basis is selected as a global features.

'''
